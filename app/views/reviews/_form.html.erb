<%= simple_form_for ([@restaurant, @review]) do |f| %>
  <%= f.input :content, placeholder: "Write something about this restaurant" %>
  <%= f.input :rating, placeholder: "How would you like to rate this restaurant(1-5)" %>
  <%= f.submit "Add a review", class: "btn btn-primary" %>
<% end %>
